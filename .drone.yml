---
kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: "checkout & install"
  image: gvacaliuc/dev:latest
  commands:
    - .config/bootstrap.sh
    - .config/dev/install-vim.sh

...
